

const storeObj = {
  bookmarks: [],
  adding: false,
  error: null,
  filter: 0
};

const addBookmark = function(){
  console.log(bookmark);
  console.log(store);
};

export default {
  addBookmark,
  storeObj
};